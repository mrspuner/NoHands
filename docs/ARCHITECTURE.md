# Архитектура NoHands

**Дата:** 2026-09-03
**Состояние:** фазы 0 и 1 закрыты, фаза 2 не начата

Документ описывает, как устроено приложение: что уже есть, что появится, и почему границы проведены именно так. Решения и их обоснования — в `DECISIONS.md`, замысел целиком — в `DESIGN.md`.

---

## 1. Что это

Менюбар-приложение для macOS, делающее две несвязанные вещи одним набором деталей:

- **Диктовка.** Зажал клавишу, наговорил, отпустил — текст появился в активном поле любого приложения
- **Созвоны.** Нажал «записать», провёл встречу, к утру получил размеченный markdown в `~/Meetings`

Общего у сценариев — распознавание речи. Всё остальное различается: источник звука, требования к задержке, объём, приватность.

---

## 2. Главное разделение: ожидание против фона

Вся архитектура держится на одном различии — **ждёте вы результата или нет**.

```
                    ЖДЁТЕ                         НЕ ЖДЁТЕ
                       │                              │
   ┌───────────────────┴──────────┐   ┌───────────────┴────────────────┐
   │  Диктовка                    │   │  Созвоны                       │
   │  бюджет задержки ~1 секунда  │   │  бюджет задержки — до утра     │
   │  20 часов речи в месяц       │   │  40–65 часов записей в месяц   │
   │  только ваш голос            │   │  чужие голоса тоже             │
   └──────────────────────────────┘   └────────────────────────────────┘
```

Из этого различия следует всё остальное: где считать, чем считать, что можно отдать наружу.

---

## 3. Поток диктовки

```
   ┌──────────┐
   │ клавиша  │  fn зажата (или fn+пробел — фиксация без удержания)
   │   fn     │────────────────┐   Esc в любой момент до вставки — отмена
   └──────────┘                ▼
                    ┌─────────────────────┐
                    │  MicrophoneRecorder │  AVAudioEngine
                    │  16 кГц, моно, WAV  │  запись в буфер
                    └──────────┬──────────┘
                               │ fn отпущена
                               ▼
                    ┌─────────────────────┐
                    │ ParakeetTranscriber │  локально, Neural Engine
                    │      ~0,2 сек       │  модель 470 МБ в памяти
                    └──────────┬──────────┘
                               │ сырой текст
                               ▼
                    ┌─────────────────────┐
                    │   DeepSeek V4 Flash │  ЕДИНСТВЕННЫЙ выход наружу
                    │      ~2,2 сек       │  чистка: паразиты, пунктуация
                    └──────────┬──────────┘
                               │ чистый текст
                               ▼
                    ┌─────────────────────┐
                    │  NSPasteboard       │  подменяем буфер обмена,
                    │  + Cmd+V            │  шлём Cmd+V, возвращаем назад
                    └─────────────────────┘

                    ┌─────────────────────┐
                    │  плавающая панель   │  получатель, уровень звука,
                    │  снизу по центру    │  стадия, причина отказа
                    └─────────────────────┘

   Суммарная задержка: ~2,4 сек по замерам фазы 0. На реальной работе
   не измерена — это первый пункт приёмки фазы 1.
```

**Почему чистка в облаке, а распознавание нет.** Локальная чистка на Qwen3 8B занимает 7,7 секунды против 2,2 у DeepSeek — при интерактивном сценарии это разница между «терпимо» и «невыносимо». Распознавание же локально быстрее облачного в четырнадцать раз, там выбор очевиден.

**Почему это не противоречит приватности.** В облако уходит только ваша собственная речь. Чужой в диктовке не бывает.

---

## 4. Поток созвонов

```
   ┌──────────┐
   │  кнопка  │  «начать запись»
   └────┬─────┘
        │
        ├──────────────────────────┬───────────────────────────┐
        ▼                          ▼                           │
   ┌─────────────────┐   ┌──────────────────┐                  │
   │ ScreenCaptureKit│   │ AVAudioEngine    │                  │
   │ системный звук  │   │ микрофон         │                  │
   │   → дорожка A   │   │   → дорожка B    │                  │
   │  (собеседники)  │   │      (я)         │                  │
   └────────┬────────┘   └─────────┬────────┘                  │
            │                      │            кнопка «стоп»  │
            └──────────┬───────────┘◄──────────────────────────┘
                       ▼
              ┌─────────────────┐
              │ очередь на диске│  ждёт ночи
              └────────┬────────┘
                       ▼
       ┌───────────────────────────────────┐
       │        ФОНОВЫЙ ВОРКЕР             │
       │                                   │
       │  дорожка A ──► диаризация         │  FluidAudio
       │              (кто когда говорил)  │
       │                                   │
       │  A и B ─────► Parakeet ──► слова  │  ~200× реального времени
       │                          с тайм-  │  71 минута за 21 секунду
       │                          кодами   │
       │                     │             │
       │                     ▼             │
       │           сопоставление сегментов │
       │           и слов по времени       │
       │                     │             │
       │                     ▼             │
       │              Qwen3 8B ──► конспект│  локально, MLX, ~2 минуты
       │                          и решения│
       └─────────────────┬─────────────────┘
                         ▼
              ~/Meetings/2026-09-01-название.md
                         │
                         ▼
              аудио → AAC 32 кбит/с
              лежит 7 дней, удаляется

   ВСЁ ЛОКАЛЬНО. Ни один байт чужой речи не покидает машину.
```

**Почему две дорожки, а не одна.** Своя дорожка с микрофона делает «меня» опознанным без всякой диаризации и без ошибок. Диаризация остаётся только на дорожке собеседников: на одного участника меньше в кластеризации и нет мест, где я говорю одновременно с кем-то.

**Почему конспекты локально, хотя чистка диктовки в облаке.** Здесь звучат чужие голоса. Участники давали согласие на запись, но не на передачу своих слов стороннему сервису — и спросить их задним числом невозможно.

---

## 5. Границы данных

```
   ┌─────────────────────────────── МАШИНА ────────────────────────────────┐
   │                                                                       │
   │   запись микрофона        распознавание         конспекты             │
   │   запись экрана           (Parakeet)            (Qwen3 8B)            │
   │   диаризация              таймкоды              база отпечатков       │
   │   архив ~/Meetings        сверка расшифровок    голосов               │
   │                                                                       │
   │                              │                                        │
   └──────────────────────────────┼────────────────────────────────────────┘
                                  │
                                  │  только сырой текст диктовки,
                                  │  только ваш собственный голос
                                  ▼
                        ┌───────────────────┐
                        │  DeepSeek V4 Flash│
                        │  чистка текста    │
                        └───────────────────┘

   Наружу не уходит:  аудио любого рода · чужая речь · транскрипты созвонов
                      отпечатки голосов · ключи API
```

---

## 6. Что написано на сегодня

```
   Package.swift
   │
   ├── Core/  (библиотека)
   │   │
   │   ├── Transcription/
   │   │     Transcriber.swift          протокол: файл → текст
   │   │     TranscriptionError.swift   отказы распознавания
   │   │     TranscriberChecks.swift    общие проверки: файл читается, текст не пуст
   │   │     ParakeetTranscriber.swift  локальный движок, актор     ◄── по умолчанию
   │   │     ScribeTranscriber.swift    облачный движок, актор      ◄── запасной
   │   │     MultipartBody.swift        сборка HTTP-запроса вручную
   │   │
   │   ├── LLM/
   │   │     DeepSeekClient.swift       чистка диктовки, актор; ключ резолвится
   │   │                                один раз и кешируется
   │   │     CleanupPayload.swift       тело запроса и разбор ответа, чистое
   │   │     CleanupError.swift         отказы чистки
   │   │
   │   ├── Audio/
   │   │     AudioInputDevice.swift     опрос устройства через CoreAudio
   │   │     MicrophoneRecorder.swift   старт/стоп/сброс, актор
   │   │     RecordingSession.swift     одна живая запись, счётчики под замком
   │   │     AudioLevel.swift           RMS → 0…1 для полосок панели
   │   │
   │   ├── Comparison/                  ТОЛЬКО ФАЗА 0
   │   │     ReferenceTranscript, TextNormalizer, WordDiff, ComparisonReport
   │   │
   │   └── Secrets/
   │         Keychain.swift             чтение ключей, только чтение
   │
   ├── Dictation/  (библиотека, Features/Dictation/)
   │     DictationMachine.swift         ВСЁ ПОВЕДЕНИЕ. Ни одного системного вызова
   │     DictationCoordinator.swift     исполняет эффекты автомата
   │     DictationConfig.swift          чтение конфига, значения по умолчанию
   │     KeyEventReader.swift           CGEvent → доменное событие; правило
   │                                    проглатывания клавиш. Чистое
   │     FnKeyMonitor.swift             CGEventTap, обёртка
   │     TextInserter.swift             активация получателя, Cmd+V, возврат буфера
   │     PasteboardSnapshot.swift       снимок и возврат буфера обмена
   │     PanelState.swift               что показывать, без текста интерфейса
   │     PanelTransition.swift          сбрасывать ли историю уровней. Чистое
   │     TargetApp.swift                приложение-получатель, без AppKit
   │     SoundPlayer.swift              системные звуки
   │     LastDictation.swift            последняя диктовка в памяти
   │
   ├── App/  (исполняемый, NoHands.app)
   │     main.swift                     NSApplication, .accessory
   │     AppDelegate.swift              сборка зависимостей, запуск координатора
   │     StatusMenu.swift               NSStatusItem и его меню
   │     DictationPanel.swift           NSPanel, неактивирующая
   │     PanelView.swift / PanelModel.swift   содержимое панели на SwiftUI
   │     Info.plist
   │
   ├── CLI/  (исполняемый nohands)
   │     NoHands.swift                  разбор команд, вывод, обработка ошибок
   │     TranscribeArguments.swift      флаги команды transcribe
   │     DictateCommand.swift           флаги команды dictate
   │
   ├── Scripts/
   │     make-app.sh                    сборка бандла и подпись
   │
   └── Tests/                           170 тестов
         CoreTests/                     звук, уровень, разбор запросов и ответов
         DictationTests/                автомат, клавиатура, буфер, конфиг, хранилище
         CLITests/                      разбор аргументов, предупреждения
```

**Границы держит компилятор, а не договорённость.** Зависимости идут строго в одну сторону: `App → Dictation → Core` и `CLI → Dictation → Core`. `Dictation` не знает о существовании панели — она приходит замыканиями из `App`. Перечисление эффектов автомата разбирается в координаторе исчерпывающе, без `default`, поэтому новый эффект становится ошибкой компиляции, а не молча потерянным действием. То же самое с состояниями панели.

**Чистое ядро отделено от системных обёрток.** `DictationMachine` — 243 строки без единого системного вызова, покрытые 26 тестами на переходы: пороги, фиксация, отмена, каждый путь отказа. Три дефекта фазы 1 нашлись ровно там, где чистое правило пряталось внутри системной обёртки: выбор проглатывания клавиши в тапе, выбор цели вставки, арифметика уровня. Все три вынесены наружу и покрыты тестами. Это самая полезная закономерность фазы: если внутри обёртки над системой есть решение, зависящее только от своих входов, — оно обязано жить снаружи.

**Протокол намеренно узкий:**

```swift
public protocol Transcriber: Sendable {
    func transcribe(audio url: URL) async throws -> String
}
```

Всё, что различается между движками — язык, словарь терминов, выбор модели — задаётся при создании конкретной реализации, а не лезет в общий интерфейс. Таймкоды в фазе 0 не нужны ни разу; они появятся в фазе 2, когда станет видно, в каком виде их удобно потреблять.

---

## 7. Зависимости

```
   NoHands
      │
      └── FluidAudio 0.14.8   ◄── ЕДИНСТВЕННАЯ внешняя зависимость
             │
             ├── Parakeet TDT v3   распознавание (используется)
             ├── диаризация        понадобится в фазе 2
             └── VAD               определение речи
```

Версия закреплена жёстко на 0.14.8. Свежие версии не устанавливаются: резолюция зависимостей намертво виснет на скачивании бинарного артефакта `NemoTextProcessing.xcframework` весом 47 МБ, нужного только для синтеза речи, которого проект не делает. Проверено трижды; тот же файл обычной загрузкой скачивается за секунды.

HTTP делается на `URLSession`, разбор аргументов — вручную. Ни то, ни другое не оправдывает новой зависимости.

---

## 8. Внешние сервисы

| Сервис | Зачем | Когда | Ключ |
|---|---|---|---|
| DeepSeek V4 Flash | чистка диктовки | используется | `nohands-deepseek` в Keychain, элемент создан с `-T` на бандл |
| ElevenLabs Scribe v2 | запасное распознавание | не используется | `nohands-elevenlabs` в Keychain |

Адрес подтверждён живым запросом: `POST https://api.deepseek.com/anthropic/v1/messages`, заголовки `x-api-key` и `anthropic-version`. Имя модели в конфиге — `deepseek-chat`, это псевдоним, который сервис разрешает в `deepseek-v4-flash`.

**Обязательное условие для DeepSeek:** параметр `thinking: {"type": "disabled"}` в каждом запросе. Без него модель уходит в рассуждения на четырнадцать тысяч символов, не доходит до ответа и тратит в тридцать пять раз больше выходных токенов — сорок центов в месяц превращаются в четырнадцать долларов.

---

## 9. Разрешения macOS

Три системных разрешения, каждое появляется в своей фазе — чтобы при поломке было понятно, что именно отвалилось.

```
   фаза 0  ──►  Микрофон              запись своей дорожки
   фаза 1  ──►  Универсальный доступ  перехват fn + вставка текста
   фаза 2  ──►  Запись экрана         системный звук через ScreenCaptureKit
```

Плюс две ручные настройки, обе проверены на живой машине:

1. Действие клавиши fn в системных параметрах переключается на «ничего не делать», иначе macOS перехватит нажатие раньше приложения.
2. Ключ DeepSeek кладётся в связку ключей **с флагом `-T` на бандл приложения**. Без него элемент знает только про `security`, и приложение как другая идентичность кода получает системный диалог при каждом чтении — посреди каждой диктовки. Подробности в журнале решений за 2026-09-03.

Приложение подписывается самоподписанным сертификатом, а не ad-hoc. Требование к коду выходит вида «этот идентификатор, подписанный этим сертификатом», без хеша бинарника, — поэтому и универсальный доступ, и запись в списке доступа связки ключей переживают пересборки.

---

## 10. Состояние по фазам

```
   ФАЗА 0  ████████████████████  закрыта
           протокол, два движка, запись микрофона, сверка расшифровок,
           инструмент прогонов. 69 тестов

   ФАЗА 1  ████████████████████  закрыта
           бандл, собираемый скриптом; менюбар; перехват fn, пробела и Esc;
           запись со стартом и стопом; чистка через DeepSeek; вставка текста;
           плавающая панель. 170 тестов. Проверено живой диктовкой

   ФАЗА 2  ░░░░░░░░░░░░░░░░░░░░  не начата
           ScreenCaptureKit, две дорожки, ночная очередь, диаризация,
           сопоставление таймкодов, генерация markdown, база отпечатков
```

**Xcode-проекта нет и не будет.** Бандл собирает `Scripts/make-app.sh`: манифест, plist и скрипт — обычный текст в гите. Менюбар-приложение без окон не выигрывает от Xcode почти ничего, а `.pbxproj` не читается, не правится и не собирается из терминала.

**Что останется в фазе 0:** каталог `Comparison` и исполняемый `nohands`. Они существуют, чтобы измерять качество распознавания, и в приложении не нужны. Удалять их не стоит: при смене движка или модели сверка понадобится снова.

---

## 11. Известные расхождения с замыслом

| Что | Замысел | Как есть |
|---|---|---|
| Задержка диктовки | ~1 сек | ~2,4 сек по замерам фазы 0, на реальной работе не измерена |
| Панель | крестик отмены и бегущая полоска | ни того, ни другого, сознательно — см. журнал за 2026-09-03 |
| Предупреждения компилятора | их нет | пять штук в `Core/Audio/`, все из фазы 0 |
| Расходы на DeepSeek | ~$0,40 в месяц | фактическое списание не сверялось |
| Основание выводов | десяток файлов | один созвон и одна диктовка |
| Конспекты против Haiku | сравнить | не сравнивались, ключа Anthropic нет |
| Проверка конспектов на выдумки | — | работает, но живёт разовым скриптом вне репозитория |

**Про предупреждения отдельно.** Два из пяти — это ошибки Swift 6, понижённые до предупреждений только потому, что в `AVFAudio` нет аннотаций конкурентности: захват `consumed` и `buffer` в замыкании конвертера. Тулчейн, где эти аннотации появятся, превратит их в отказ сборки. Долг до фазы 2, чинить нужно на машине с микрофоном — тракт реального времени вслепую не перестраивают.
